unbind %
bind | split-window -h -c "#{pane_current_path}"

unbind '"'
bind - split-window -v -c "#{pane_current_path}"

unbind r
bind r source-file ~/.tmux.conf

unbind .
bind . command-prompt -p "Rename session:" -I "#{s/^[0-9]+-//:session_name}" "run-shell -b '~/.config/tmux/scripts/rename_session_prompt.sh \"%%\"; tmux refresh-client -S'"

bind h split-window -hb -c "#{pane_current_path}"
bind j split-window -v -c "#{pane_current_path}"
bind k split-window -vb -c "#{pane_current_path}"
bind l split-window -h -c "#{pane_current_path}"

bind-key -n -r C-S-h resize-pane -L 5
bind-key -n -r C-S-j resize-pane -D 5
bind-key -n -r C-S-k resize-pane -U 5
bind-key -n -r C-S-l resize-pane -R 5

bind-key -n C-f resize-pane -Z
unbind-key -n -q C-n
bind-key -n F11 next-window
bind-key -n C-o new-window -c "#{pane_current_path}"
bind-key -n C-q kill-window
bind-key -n C-v copy-mode
unbind-key -n -q M-t
bind-key -n S-F11 detach-client
bind-key -T copy-mode-vi S-F11 detach-client

set -g mouse on
set -g mode-keys vi
# Status
set -g status-interval 60
set -g escape-time 10
set -g status-style fg=default,bg=default
# Terminal colors
set -g default-terminal "tmux-256color"
set -s terminal-features[100] '*:RGB:usstyle'

# Clipboard
set -g allow-passthrough on

# Enable CSI u
set -g extended-keys on
set -s terminal-features[101] 'xterm*:extkeys'

# Enable focus events
set -g focus-events on

# Enable gapeless window
set -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1

# History buffer size
set -g history-limit 100000

### Plugins
source-file ~/.config/tmux/plugins.conf

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
run '~/.tmux/plugins/tpm/tpm' # Initialize TPM

set -g status-left-length 400
set -g status-left "#(~/.config/tmux/tmux-status/left.sh \"#{session_id}\" \"#{client_width}\" \"#{status-bg}\") "

set-hook -g client-attached[100] 'refresh-client -S'
set-hook -g client-resized[100] 'refresh-client -S'
set-hook -g client-session-changed[100] 'refresh-client -S'

set-hook -g session-created[100] 'run-shell -b "~/.config/tmux/scripts/session_created.sh"'
set-hook -g session-closed[100] 'run-shell -b "~/.config/tmux/scripts/session_manager.py ensure"'
run-shell -b "~/.config/tmux/scripts/session_created.sh"

unbind 0
unbind 1
unbind 2
unbind 3
unbind 4
unbind 5
unbind 6
unbind 7
unbind 8
unbind 9
bind 1 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 1"
bind 2 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 2"
bind 3 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 3"
bind 4 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 4"
bind 5 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 5"
bind 6 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 6"
bind 7 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 7"
bind 8 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 8"
bind 9 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 9"
bind 0 run-shell -b "~/.config/tmux/scripts/move_window_to_session.sh 10"

bind-key -n C-s run-shell -b "~/.config/tmux/scripts/new_session.sh"

bind-key -n C-1 select-window -t 1
bind-key -n C-2 select-window -t 2
bind-key -n C-3 select-window -t 3
bind-key -n C-4 select-window -t 4
bind-key -n C-5 select-window -t 5
bind-key -n C-6 select-window -t 6
bind-key -n C-7 select-window -t 7
bind-key -n C-8 select-window -t 8
bind-key -n C-9 select-window -t 9

bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

unbind-key -n -q M-!
unbind-key -n -q M-@
unbind-key -n -q 'M-#'
unbind-key -n -q 'M-$'
unbind-key -n -q M-%
unbind-key -n -q M-^
unbind-key -n -q M-&
unbind-key -n -q M-*
unbind-key -n -q 'M-('

bind-key -T copy-mode-vi C-1 select-window -t 1
bind-key -T copy-mode-vi C-2 select-window -t 2
bind-key -T copy-mode-vi C-3 select-window -t 3
bind-key -T copy-mode-vi C-4 select-window -t 4
bind-key -T copy-mode-vi C-5 select-window -t 5
bind-key -T copy-mode-vi C-6 select-window -t 6
bind-key -T copy-mode-vi C-7 select-window -t 7
bind-key -T copy-mode-vi C-8 select-window -t 8
bind-key -T copy-mode-vi C-9 select-window -t 9

bind-key -T copy-mode-vi M-1 select-window -t 1
bind-key -T copy-mode-vi M-2 select-window -t 2
bind-key -T copy-mode-vi M-3 select-window -t 3
bind-key -T copy-mode-vi M-4 select-window -t 4
bind-key -T copy-mode-vi M-5 select-window -t 5
bind-key -T copy-mode-vi M-6 select-window -t 6
bind-key -T copy-mode-vi M-7 select-window -t 7
bind-key -T copy-mode-vi M-8 select-window -t 8
bind-key -T copy-mode-vi M-9 select-window -t 9

unbind-key -T copy-mode-vi -q M-!
unbind-key -T copy-mode-vi -q M-@
unbind-key -T copy-mode-vi -q 'M-#'
unbind-key -T copy-mode-vi -q 'M-$'
unbind-key -T copy-mode-vi -q M-%
unbind-key -T copy-mode-vi -q M-^
unbind-key -T copy-mode-vi -q M-&
unbind-key -T copy-mode-vi -q M-*
unbind-key -T copy-mode-vi -q 'M-('

bind-key -T copy-mode-vi F1 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 1 --client '#{client_name}'"
bind-key -T copy-mode-vi F2 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 2 --client '#{client_name}'"
bind-key -T copy-mode-vi F3 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 3 --client '#{client_name}'"
bind-key -T copy-mode-vi F4 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 4 --client '#{client_name}'"
bind-key -T copy-mode-vi F5 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 5 --client '#{client_name}'"
bind-key -T copy-mode-vi F6 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 6 --client '#{client_name}'"
bind-key -T copy-mode-vi F7 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 7 --client '#{client_name}'"
bind-key -T copy-mode-vi F8 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 8 --client '#{client_name}'"
bind-key -T copy-mode-vi F9 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 9 --client '#{client_name}'"
bind-key -T copy-mode-vi F10 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 10 --client '#{client_name}'"

bind-key -n F1 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 1 --client '#{client_name}'"
bind-key -n F2 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 2 --client '#{client_name}'"
bind-key -n F3 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 3 --client '#{client_name}'"
bind-key -n F4 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 4 --client '#{client_name}'"
bind-key -n F5 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 5 --client '#{client_name}'"
bind-key -n F6 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 6 --client '#{client_name}'"
bind-key -n F7 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 7 --client '#{client_name}'"
bind-key -n F8 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 8 --client '#{client_name}'"
bind-key -n F9 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 9 --client '#{client_name}'"
bind-key -n F10 run-shell -b "~/.config/tmux/scripts/session_manager.py switch 10 --client '#{client_name}'"

bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "~/.config/tmux/scripts/copy_to_clipboard.sh"
